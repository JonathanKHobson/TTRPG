# War Table v3 Implementation Drift Log

This log tracks mismatches among intended contract, current code behavior, local behavior, hosted behavior, QA expectations, and rules-source text.

## Entries

| Drift ID | Area | Mismatch | Evidence | Status | Resolution / Next Step |
| --- | --- | --- | --- | --- | --- |
| DRIFT-001 | Battle staging copy | Runtime behavior was staging-first, but CTA / setting copy still implied an immediate battle. | Audit WT-101, local code review in `setWarReportActionState()` and Settings copy. | Resolved local | Updated UI copy and CTA text. Hosted build still needs build-label parity confirmation. |
| DRIFT-002 | History visibility | Product contract wanted current-session visibility before first battle, but persisted storage remained flat battle records only. | Audit WT-102, History rendering behavior in local app. | Resolved local | Added a derived runtime-only placeholder session. Placeholder remains intentionally non-persisted. |
| DRIFT-003 | Reset / New Session semantics | User expectation and audit spec were narrower than current code behavior. | Audit WT-103 and local code review of `resetAll()` / `handleStartNewSessionFromUi()`. | Resolved local | `Reset` and `New Session` now follow the stabilization contract. Hosted confirmation still required. |
| DRIFT-004 | Battle context rules | Visible context toggles behaved like nearby-only support instead of direct active-matchup modifiers for the approved ticketed cases. | Audit WT-104 and supplements. | Resolved local | Added a direct context layer and legacy-disabled the old nearby-only entries for those mechanics. |
| DRIFT-005 | Rules data: Mel's Army | Raw canon text listed `Thieves/Bandits` immunity, but parsed runtime matchups omitted it. | Matchup supplement and embedded `RULES.armyClasses`. | Resolved local | Patched embedded rules data before index build. |
| DRIFT-006 | Rules data: Drow day/night math | Current weather normalization collapsed time-of-day nuance and the hardcoded Drow values did not match the approved rule. | Dice supplement plus static code review. | Resolved local | Added time-aware weather matching and updated Drow special handling. |
| DRIFT-007 | Local vs hosted parity | Local build is now labeled, but hosted parity has not yet been re-run against the March 8, 2026 army-logic P1 build label. | Audit WT-105 plus WT-119/WT-125 local execution. | Open | Compare local and hosted builds using the visible build label and the Playwright smoke path once `WAR_TABLE_HOSTED_URL` is available. |
| DRIFT-008 | Dirty-guard completion semantics | The stabilization contract expected one guarded discard action to complete the requested transition, but the local implementation only reverted the first dirty side in scope. | Stabilization verification against WT-103 / WT-106. | Resolved local | Dirty-guard discard now reverts every dirty side in scope before re-running the blocked action. |
| DRIFT-009 | Blank-side initialization | Empty battle sides were expected to remain blank until user input, but class selects defaulted to a real class during bind and polluted dirty/load behavior. | Post-pass loader regression triage and browser repro. | Resolved local | Added blank class options and bound class selects from existing state instead of DOM defaults. |
| DRIFT-010 | Auto-battle runtime safety | Auto-battle was expected to stop cleanly, but a missing PHENOMENA helper could throw mid-loop and leave the modal wedged in `auto-running`. | Local browser repro with `Forest Fey` vs `Archers`; runtime error `weatherTagDeltaForClass is not defined`. | Resolved local | Restored the helper and added auto-battle try/catch/finally plus force-finalize Stop recovery. |
| DRIFT-011 | History interaction clarity | History grouping existed, but the collapsed/expanded hierarchy was too visually weak to read reliably as session -> turn -> battle. | Post-pass usability review and local renderer inspection. | Resolved local | Strengthened toggle affordances, indentation, and per-level visual contrast without changing the underlying history data model. |
| DRIFT-012 | History layout / empty-state language | History was expected to behave like primary page content, but it inherited the generic `420px` inner scroll cap and exposed internal runtime terminology in user-facing copy. | Post-pass usability review and local CSS inspection. | Resolved local | Moved History to its own page layout treatment and replaced technical empty-state copy with plain-language messaging. |
| DRIFT-013 | Dev guide: primary action button labels | Dev guide said `Start Battle` / `Battle Again`; shipped code uses `Start Battle Roll` / `Run Battle Again`. | AUDIT_BATTLE_FLOW_2026-03-06 FINDING-001. | Resolved | Dev guide lines updated. No code change needed. |
| DRIFT-014 | Dev guide: round-limit selector description | Dev guide referenced bare numeric values only; shipped HTML uses `Until stop` (empty value) default and `X rounds` display text. | AUDIT_BATTLE_FLOW_2026-03-06 FINDING-002. | Resolved | Dev guide updated to describe actual option values and default sentinel. |
| DRIFT-015 | Dev guide: history toggle element type | Dev guide fragile area #9 said session/turn groups use "real toggle buttons with `aria-expanded` and `aria-controls`"; shipped code uses native `<details>/<summary>` with `historyToggleBtn` CSS class. | AUDIT_BATTLE_FLOW_2026-03-06 FINDING-003 and FINDING-004. | Resolved | Dev guide corrected; explicit `aria-expanded` mirroring added to summary elements and kept in sync via toggle listener. |
| DRIFT-016 | War Report modal initial focus | Native dialog focus behavior landed on ✕ close button; contract expected primary action button to receive initial focus. | AUDIT_BATTLE_FLOW_2026-03-06 FINDING-005. | Resolved local | `openWarReportModal` now explicitly focuses `#warReportBattleAgainBtn` (staging/result) or `#warReportClose` Stop (auto-running) after `showModal()`. |
| DRIFT-017 | Army-logic hosted verification | The local army-logic lane is green through the Fire Cult weather-lock and nearby-action audit pass, but this workspace still cannot execute hosted smoke because `WAR_TABLE_HOSTED_URL` is unset. | WT-119 through WT-130 local execution plus March 8, 2026 full `chromium-local` Playwright run on build `2026-03-08-army-logic-p2`. | Open (waived for local continuation) | Continue local implementation under the explicit waiver recorded in the roadmap; re-run hosted smoke as soon as `WAR_TABLE_HOSTED_URL` is available. |
| DRIFT-018 | Shield-Brethren charge suppression | The narrow Shield-Brethren parity pass now covers direct bulwark math, nearby aura support, and represented surprise blocking, but there is still no first-class cavalry-charge subsystem to suppress. | March 8, 2026 Shield-Brethren implementation review against `War_Encounter_Rules_2026_v4.md`. | Open (documented deferral) | Keep this deferred until a dedicated cavalry-charge bonus path exists; do not improvise hidden state or new UI just to simulate it. |
| DRIFT-019 | Battle first-run tone | Audit expected a setup-oriented first impression, but local behavior opened in an error-first `Not ready` state before user action. | March 8 UX audit F-01 and local browser repro. | Resolved local | `renderBattleReadinessMessage()` now keeps missing-setup states neutral while preserving the existing disabled Battle gate and escalation path for true invalid-action errors. |
| DRIFT-020 | Feedback announcement semantics | Visible validation and save/import feedback existed, but the live-region contract was inconsistent across battle, modal, and utility surfaces. | March 8 UX audit F-06 plus local DOM inspection. | Resolved local | Added explicit alert/status semantics to the main validation, modal error, toast, and import feedback surfaces while preserving the existing `warReportAutoStatus` pattern. |
| DRIFT-021 | Dirty-guard initial focus | The dirty-state modal opened with focus on the close icon instead of the safest user decision. | March 8 UX audit F-07 and local focus check. | Resolved local | `openDirtyGuard()` now explicitly focuses `#dirtyGuardCancel` (`Stay`) after `showModal()` with safe fallbacks. |
| DRIFT-022 | Favicon console noise | Local/browser runs were generating avoidable missing-asset console noise even though the app otherwise loaded cleanly. | March 8 UX audit QA note and browser console check. | Resolved local | Added an inline icon in the document head so the app no longer requests a missing `favicon.ico`. |
