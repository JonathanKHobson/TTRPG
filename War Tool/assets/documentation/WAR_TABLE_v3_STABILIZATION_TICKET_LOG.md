# War Table v3 Stabilization Ticket Log

This is the living implementation log for the March 7-8, 2026 stabilization pass.

## Entries

| Issue ID | Category | Source | Reproduction | Expected | Actual Before Fix | Status | Owner | Tests Added | Outcome |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| WT-101 | UX / battle flow | Audit ticket bundle | Click `Battle` with War Report enabled. | War Report opens in staging and clearly states that no round has run yet. | Modal staging existed, but primary CTA copy still implied an immediate battle. | Fixed | Frontend | `War Report staging does not append history until Start Battle Roll` | Fixed |
| WT-102 | History / session trust | Audit handoff | Open History before the first recorded battle or after clearing history. | Current runtime session is visible immediately and clearly marked runtime-only. | No real grouped session block existed for an empty current session. | Fixed | Frontend | `History shows a runtime-only empty session placeholder` | Fixed |
| WT-103 | State model / battle flow | Audit handoff | Use `Reset` or `New Session` after setting up sides. | `Reset` clears only battle setup; `New Session` clears sides, resets runtime session + weather, preserves history. | `Reset` also reset settings; `New Session` only touched runtime metadata. | Fixed | Frontend | `Reset clears battle setup but preserves settings`; `New Session clears setup, resets battle and weather turn labels, and shows a new placeholder` | Fixed |
| WT-104 | Rules / battle context | Audit handoff | Toggle `Home Territory`, `Forest Environment`, or `Flanked by Two Enemies` without nearby rows. | Ticketed context mechanics directly change the active matchup and show in breakdown. | Visible toggles only fed nearby assist/hinder actions, so the active pool often did not change. | Fixed | Frontend | `Ticketed direct-context mechanics change the active pool and breakdown` | Fixed |
| WT-106 | UX / unsaved changes | Audit handoff | Change saveable army fields, then reset, swap, load over a side, or start a new session. | App offers Save/Update, Discard, or Cancel. | Destructive actions bypassed saveable-army dirty checks. | Fixed | Frontend | `Dirty army guard appears before destructive battle actions` | Fixed |
| WT-107 | UX / unsaved settings | Audit handoff | Change Settings, then leave Settings or refresh. | In-app navigation is guarded and refresh gets best-effort browser warning. | Dirty indicator and leave guard were absent. | Fixed | Frontend | Covered indirectly by new Playwright harness and `beforeunload` wiring; manual browser sanity still required | Partially fixed |
| WT-108 | History / transparency | Audit handoff | Record a battle, rename an army later, then reopen History. | History keeps the battle-time army names. | History card titles were class-only and lost the user’s battle-time naming context. | Fixed | Frontend | `History snapshots battle-time army names even after later rename` | Fixed |
| WT-110 | Rules data / modifier order | Audit supplements | Compare Mutants, Drow, Mel's Army, and matchup alias cases against rules notes. | Ticketed data and modifier-order fixes land without broad rules rewrite. | Mutants did not add a real extra `d10`; Mel's Army immunity data was missing; Drow day/night math was simplified; Vampires alias handling was absent. | Fixed | Frontend | `Ticketed direct-context mechanics change the active pool and breakdown` plus manual engine review | Partially fixed |
| WT-111 | UX / unsaved changes | Stabilization verification | Set up both sides with unsaved army fields, then click `Reset` or `New Session` and choose `Discard`. | A single discard action reverts all dirty armies in scope and completes the requested transition. | Dirty-guard discard only reverted the first dirty side, leaving the second side populated and forcing repeated guard loops. | Fixed | Frontend | `Reset clears battle setup but preserves settings`; `New Session clears setup, resets battle and weather turn labels, and shows a new placeholder` | Fixed |
| WT-112 | Loader / blank-state regression | Post-pass bug report | Start from an empty or untouched creating side, then use battle-panel Load or Armies-tab load arrows. | Empty sides do not prompt to save, and the selected army loads reliably. | Hidden class selects defaulted to a real class and untouched creating drafts counted as dirty, causing false save prompts and missed loads. | Fixed | Frontend | `Blank and untouched creating sides load saved armies without false dirty prompts` | Fixed |
| WT-113 | Auto-battle freeze / runtime error | Post-pass bug report | Run auto-battle into PHENOMENA weather or a matchup that exercises implied-tag scoring, then try Stop after the session wedges. | Auto-battle either completes normally or stops cleanly with a usable modal. | Missing `weatherTagDeltaForClass` caused a runtime exception, leaving auto-battle stuck in `auto-running` and Stop unable to recover. | Fixed | Frontend | `Auto-battle survives PHENOMENA weather and Stop force-finalizes a stale running session` | Fixed |
| WT-114 | Loader / runtime tactical carry-over | Post-pass bug report | Replace a loaded army after changing manual/context/runtime tactical controls on that side. | Loading a saved army starts from clean side-specific tactical state. | New loads inherited stale manual, nearby, context, and commit state from the previous occupant. | Fixed | Frontend | `Loading a saved army clears side runtime tactical state` | Fixed |
| WT-115 | History UX / navigation hierarchy | Post-pass usability review | Open History with recorded sessions and turns, then try to navigate sessions, turns, and battle cards. | Sessions and turns are clearly bundled, clearly collapsed by default, and easy to expand independently. | The hierarchy existed in code, but the toggle affordance was visually weak and the page read like a flat wall of content. | Fixed | Frontend | `History sessions and turns are collapsed by default and expand independently` | Fixed |
| WT-116 | History layout / copy clarity | Post-pass usability review | Open History with no records or many records. | Empty-state copy is plain-language and the History tab uses page-level space instead of a small inner scroller. | Empty-state copy exposed internal runtime terminology, and `#historyList` inherited the shared `420px` scroll cap. | Fixed | Frontend | `Empty History shows plain-language placeholder copy`; `History uses page layout instead of the shared 420px inner scroller` | Fixed |
| WT-117 | Accessibility / history toggles | AUDIT_BATTLE_FLOW_2026-03-06 FINDING-004 | Open History with sessions and turns, then expand/collapse using keyboard or AT. | `<summary>` elements report correct expand/collapse state via `aria-expanded` in all tested browsers. | Native `<details>/<summary>` exposes `aria-expanded` implicitly in Chromium but inconsistently across Firefox + NVDA. | Fixed | Frontend | `History sessions and turns are collapsed by default and expand independently` | Fixed |
| WT-118 | UX / modal focus | AUDIT_BATTLE_FLOW_2026-03-06 FINDING-005 | Open War Report staging with keyboard only. | Initial focus lands on the primary action button (`Start Battle Roll` or `Run Battle Again`), not the ✕ close button. | Native dialog focused the first focusable descendant in DOM order (the ✕ button). | Fixed | Frontend | `War Report modal opens with focus on primary action button in staging mode` | Fixed |
| WT-119 | Army-logic roadmap / baseline | Army Logic Stabilization Roadmap | Review the PRD lane, confirm existing P0 baseline coverage, and capture the current build/hosted parity state. | Roadmap exists in-repo, logs are seeded, local build label is current, and hosted parity status is recorded. | No staged roadmap doc existed and the new army-logic queue had not been logged. | Fixed | Frontend | Existing direct-context regression plus full local Playwright suite | Fixed |
| WT-120 | Rules / direct context | Army Logic Stabilization Roadmap | Attack with Bandits while `Hidden / Surprise position` is enabled, with and without `Enemy has intel on hidden`. | Attacking Bandits gain `+1 die` only when hidden and not countered by enemy intel; defender-side Bandits do not gain the bonus. | Hidden state existed in context UI but Bandits had no direct active bonus. | Fixed | Frontend | `Bandits hidden bonus only applies to attacking bandits without enemy intel` | Fixed |
| WT-121 | Rules / class dice shape | Army Logic Stabilization Roadmap | Run Gunners at size thresholds 9, 10, 19, and 20 with a stable non-matchup opponent. | Mixed dice replace eligible `d6` dice with `d10` dice for the current battle and the loadout is visible in Dice Math Breakdown. | Gunners used a flat die list and never upgraded into the intended mixed pool. | Fixed | Frontend | `Gunners mixed dice upgrades follow the 9/10/19/20 size thresholds` | Fixed |
| WT-122 | Rules / post-resolution mutation | Army Logic Stabilization Roadmap | Battle with Vampires in direct daytime sun, then review STR, inline result, War Report, and history detail. | Sunlight still penalizes battle math and also applies a persistent post-battle STR loss with visible reporting. | Sunlight only reduced effective battle STR for the round and did not persist the loss or report it clearly after resolution. | Fixed | Frontend | `Vampire sunlight triggers persistent STR loss and visible reporting`; `Vampire sunlight only triggers in direct daytime sun` | Fixed |
| WT-123 | Rules / bespoke matchup rider | Army Logic Stabilization Roadmap | Run Snow Elves vs Forest Fey / Treants and Fire Cult vs Snow Elves with deterministic neutral weather. | The approved bespoke rider adds on top of existing matchup logic and is visible in Dice Math Breakdown. | Existing matchup data covered only the generic resistance/deadly layer; the extra rider was missing. | Fixed | Frontend | `Snow Elves and Fire Cult bespoke matchup riders stack on top of existing matchup logic` | Fixed |
| WT-124 | Rules / post-resolution mutation | Army Logic Stabilization Roadmap | Let Hel’s Legion win and lose deterministic quick battles, then review losses and War Report messaging. | Only Hel’s Legion wins double the opposing loss, recompute size from pre-round values, and report the amplification. | Hel’s Legion used normal result losses with no execution-rule amplification. | Fixed | Frontend | `Hel’s Legion only doubles losses on its own wins and reports the amplification` | Fixed |
| WT-125 | Rules / post-resolution mutation | Army Logic Stabilization Roadmap | Let an Undead army beat an eligible non-special class, then compare size/result/War Report/history. | Eligible Undead wins add the defeated eligible units to current size and report the growth. | Undead wins used normal losses only and never regrew size from defeated non-special units. | Fixed | Frontend | `Undead victory growth only applies against eligible non-special classes and is reported` | Fixed |
| WT-126 | Rules / post-resolution mutation | Army Logic Stabilization Roadmap | Let Fiends lose units, then resolve end-of-battle conversion against both eligible and blocked opponents. | Fiends only roll when they lost units; on `d6=6` they convert up to 10 enemy units unless resistance/immunity blocks it, and the result is reported. | Fiends had no end-of-battle conversion roll or visible messaging. | Fixed | Frontend | `Fiends conversion requires lost units, converts on d6=6, and reports the outcome`; `Fiends conversion is blocked by enemy resistance or immunity to Fiends` | Fixed |
| WT-127 | Transparency / reporting | Army Logic Stabilization Roadmap | Review inline result, War Report, and history after matchup, nearby, and post-battle class effects trigger. | Implemented class effects appear under stable labels across result, War Report, and history. | P0 reporting scaffolding existed, but matchup/doctrine/nearby summaries were not consistently flowing into additive reporting. | Fixed | Frontend | `War Report and history now surface matchup-driven class effects alongside result output` | Fixed |
| WT-128 | Weather / optional spike | Army Logic Stabilization Roadmap | Run Fire Cult battles from disallowed weather and through auto-advance drift / PHENOMENA transitions. | Fire Cult constrains battle weather to `CLEAR` / `DRY` / `HEAT` through pre-battle normalization and post-battle clamp messaging without changing weather-order flow or adding UI. | Optional weather-lock behavior was still deferred and Fire Cult battles could start or drift into disallowed weather states. | Fixed | Frontend | `Fire Cult normalizes disallowed battle weather into the dry-fire lane and reports it`; `Fire Cult clamps post-battle weather drift while non-Fire-Cult battles keep the original weather flow`; `Fire Cult auto-advance stays within CLEAR, DRY, and HEAT across repeated rounds` | Fixed |
| WT-129 | Rules / direct-context + nearby parity | Next Phase After P0 Army Logic | Review Shield-Brethren as the main defender and as nearby allied support, then compare direct-context math and represented surprise immunity. | Defender-side bulwark pressure works without manual nearby setup, allied nearby aura still grants `+1` die when defending, and represented surprise-positioning bonuses are ignored. | Shield-Brethren city/fort/choke pressure was still modeled as a manual nearby effect and represented surprise immunity was not enforced. | Fixed | Frontend | `Shield-Brethren city bulwark, nearby aura, and represented surprise immunity are applied` | Fixed |
| WT-130 | Nearby-action parity / messaging polish | Next Phase After P0 Army Logic | Audit the remaining nearby-action classes after Shield-Brethren and compare role/range/doctrine gating, queueing, roll ops, and reporting against the current architecture. | Remaining nearby-action classes are either confirmed, fixed, or explicitly deferred with rationale. | The broad nearby-action audit had not run yet beyond the Shield-Brethren pass, and defender-only hinder actions were still role-gated against the affected side instead of the source side. | Fixed | Frontend | `Nearby archers and mages keep current parity, including Cavalry exception and nearby Anti-Magic suppression`; `Mage arcane volley keeps the Spy exception while nearby reporting remains intact`; `Nearby roll-operation support keeps current remove-lowest and remove-highest behavior`; `Builder nearby actions keep repair math and queued siege-prep runtime behavior`; `Thieves and Mel nearby STR-transfer effects keep current battle-time math and reporting`; `Forest Fey restore and Treant hidden surprise keep current nearby parity`; `Battlefield Weavers and Ward-Smiths nearby actions respect doctrine gating and keep current math` | Fixed |
| WT-138 | UX / battle onboarding | March 8 UX audit F-01 | Load the app with no saved setup and inspect the center Battle card before any user action. | Battle is disabled until ready, but the user sees neutral setup guidance instead of an error-first `Not ready` block. | Fresh load immediately framed missing required inputs as a large warning/error list. | Fixed | Frontend | `Fresh load shows setup guidance instead of an error-styled not-ready block` | Fixed |
| WT-139 | UX writing / helper copy | March 8 UX audit F-05 | Review helper copy in Battle, Settings, dirty-guard messaging, and War Report staging after the UX follow-up. | High-friction copy uses task language and keeps only necessary domain terms. | Several helper lines sounded like internal docs or implementation notes rather than user guidance. | Fixed | Frontend | Existing Battle / War Report flow tests plus manual UX copy review | Fixed |
| WT-140 | Accessibility / live feedback semantics | March 8 UX audit F-06 | Trigger Battle validation, modal validation, save feedback, and import/export feedback using keyboard and DOM inspection. | Blocking errors use assertive alert semantics and passive confirmations use polite status semantics across the shared surfaces. | Feedback semantics were inconsistent: some messages were visible but lacked explicit live-region contracts. | Fixed | Frontend | `Validation and modal error surfaces expose accessible live-region semantics` | Fixed |
| WT-141 | UX / dirty-state modal focus | March 8 UX audit F-07 | Change Settings, attempt to leave the tab, and inspect focus when the dirty guard opens. | Initial focus lands on `Stay` or another safe action rather than the close icon. | Native dialog focus landed on the close icon first. | Fixed | Frontend | `Settings dirty guard focuses Stay by default` | Fixed |
| WT-142 | Asset hygiene / console trust | March 8 UX audit QA note | Load the app and inspect console/network behavior for favicon requests. | The document head provides an icon and the app does not request a missing `favicon.ico`. | Browser runs generated avoidable `favicon.ico` `404` console noise. | Fixed | Frontend | `Head provides an icon without a favicon.ico console 404` | Fixed |

## Planning / Pre-production Entries

These entries are planning-only. They do not represent shipped implementation.

| Issue ID | Category | Source | Reproduction | Expected | Actual Before Fix | Status | Owner | Tests Added | Outcome |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| WT-131 | Planning / PRD | Three-pool calibration lane | Review the current `threePool` model, quality-band discussion, and documentation needs. | A root-level PRD / dev handoff exists for the next lane and is sufficient for later implementation. | No dedicated planning artifact existed for three-pool calibration, quality tags, ceilings, and warning language. | Drafted | Product / Frontend | N/A | Planning package defined |
| WT-132 | Planning / rules parity | Three-pool calibration lane | Review simulator-truth changes that should eventually reach the master rulebook. | A persistent rulebook suggestions log exists and is seeded with the initial parity items from this lane. | Simulator-to-rulebook recommendations were discussed in conversation but not tracked in a dedicated parity log. | Drafted | Product / Rules | N/A | Suggestions log defined |
| WT-133 | Planning / battle calibration | Three-pool calibration lane | Review `Total dice from Size + STR` and define quality bands. | Quality tags from `Squalid` through `Mythical` are fully specified from `Total dice` alone. | No approved quality-tag mapping existed for the later implementation pass. | Implemented | Product / Frontend | `Battle scale line reflects Total dice and quality tag`; `Special and Faction classes remain warning-only above Standard ceilings` | Mapping shipped in simulator |
| WT-134 | Planning / validation | Three-pool calibration lane | Review class quality ceilings and intended enforcement behavior. | Class ceilings are specified and the enforcement mode is defined as pre-battle block, not save-time block. | Ceiling intent existed in conversation but not in a developer-ready implementation artifact. | Implemented | Product / Frontend | `Rebels above Poor are blocked from battle`; `Standard classes above Aristocratic are blocked while save/update still works` | Ceiling behavior shipped in simulator |
| WT-135 | Planning / UX warnings | Three-pool calibration lane | Review warning language for oversized armies. | `Very Wealthy+` armies have approved future-facing Supply warning language for later UI work. | Supply warning language was not yet documented in a stable implementation handoff. | Implemented | Product / UX | `Very Wealthy+ warnings render in Battle and Armies without changing battle math` | Warning ladder shipped in simulator |
| WT-136 | Planning / terminology | Three-pool calibration lane | Review compact battle labels for the three-pool surfaces. | Candidate compact UI terms for `Usable dice`, `Dice to roll`, and `Dice to roll cap` are documented for later UX review and implementation. | Current labels are technically accurate but still awkward in compact battle surfaces. | Planned | Product / UX | N/A | Terminology review queued |
| WT-137 | Planning / regression and rollout | Three-pool calibration lane | Review how quality tags, warnings, and ceiling enforcement should land without breaking current `threePool` behavior. | A phased roadmap, regression scope, and rollout guardrails exist before code work opens. | No staged implementation lane existed yet for the three-pool calibration work. | Implemented | Product / QA / Frontend | `Quality tags ignore matchup/weather/doctrine/manual changes`; `Ceiling blocks apply in both direct battle and War Report battle-start flows` | First implementation slice shipped against the planned rollout guardrails |
